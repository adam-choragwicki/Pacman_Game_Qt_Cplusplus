#include "foodball.h"

Foodball::Foodball(const Coordinates& coordinates) : AbstractBall(coordinates, DIAMETER)
{
    //    rect_.setSize(QSize(DIAMETER, DIAMETER));
    //    rect_.translate(OFFSET_X, OFFSET_Y);

//    setRect(0, 0, DIAMETER, DIAMETER);
}
