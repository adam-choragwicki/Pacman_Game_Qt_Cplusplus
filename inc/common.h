#pragma once

enum class Direction
{
    none,
    left,
    up,
    down,
    right
};

enum class GameResult
{
    noResultYet,
    gameLost,
    gameWin
};

enum class Key
{
    left, right, up, down, pause, space
};
